# Prefer compressed RAM (zram) over evicting file cache.
# Swap on this system is /dev/zram0 at priority 100; the disk
# partition /dev/nvme0n1p2 is a priority -1 overflow net only.
# 180 is the Arch Wiki recommendation for zram-backed systems.
vm.swappiness = 180
